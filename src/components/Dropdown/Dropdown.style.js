import Styled from 'styled-components';

const StyledSelect = Styled.select`
    font-family: 'Catamaran';
    font-size: 1em;
    margin-right: 5px;
    display: block;
`;

const StyledLabel = Styled.label`
    font-size: 0.7em;
    display: block;
`;

const StyledOption = Styled.option``;

export { StyledSelect, StyledOption, StyledLabel };
